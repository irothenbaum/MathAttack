const GameSettings = {
  // shared:
  minValue: 0,
  maxValue: 100,
  decimalPlaces: 0,
  autoSubmit: true,
  allowNegative: true,

  // classic:
  equationDuration: 5000,
  classicNumberOfRounds: 6,

  // marathon:
  numberOfStrikes: 3,

  // estimate:
  estimateItems: 4,
}

export default GameSettings
